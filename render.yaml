services:
  - type: web
    name: car-plate-app
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    build:
      aptPackages: true
